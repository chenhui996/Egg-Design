import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import { library } from '@fortawesome/fontawesome-svg-core';
import { fas } from '@fortawesome/free-solid-svg-icons';
library.add(fas);
import Icon from "./icon";

<Meta title="组件总览/Icon 图标" component={Icon} />

# Icon Component

语义化的矢量图形。使用图标组件。

提供了一套常用的图标集合 基于 react-fontawesome。

支持 react-fontawesome的所有属性 可以在这里查询 https://github.com/FortAwesome/react-fontawesome#basic

支持 fontawesome 所有 free-solid-icons，可以在这里查看所有图标 https://fontawesome.com/icons?d=gallery&s=solid&m=free

## Icon header

```js
import { Icon } from "egg-design";
import { library } from '@fortawesome/fontawesome-svg-core';
import { fas } from '@fortawesome/free-solid-svg-icons';
library.add(fas);
```

## 默认 Icon

<Canvas>
  <Story name="Icon">
    <Icon icon="coffee" theme="primary" size="2x" />
    <Icon icon="times" theme="secondary" size="2x" />
    <Icon icon="coffee" theme="success" size="4x" />
    <Icon icon="times" theme="info" size="4x" />
    <Icon icon="coffee" theme="warning" size="6x" />
    <Icon icon="times" theme="danger" size="6x" />
    <Icon icon="coffee" theme="light" size="8x" />
    <Icon icon="times" theme="dark" size="8x" />
  </Story>
</Canvas>

## API

通过设置 Icon 的属性来产生不同的按钮样式，推荐顺序为: icon -> theme -> size。

**Icon 的属性说明如下：**

<ArgsTable of={Icon} />
